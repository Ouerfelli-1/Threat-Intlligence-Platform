FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# TIP API (port 8002)
EXPOSE 8002

CMD ["uvicorn", "tip.api.main:app", "--host", "0.0.0.0", "--port", "8002"]
