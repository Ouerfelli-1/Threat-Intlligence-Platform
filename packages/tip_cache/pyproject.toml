[project]
name = "tip-cache"
version = "0.1.0"
description = "Async Redis helpers"
requires-python = ">=3.11"
dependencies = [
    "redis>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tip_cache"]
